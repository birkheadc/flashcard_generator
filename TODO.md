# TODO

- Change default template to have audio on front, and center all items
- Fix Anki Deck Name input text not very visible when input not selected (white on off-white)
- Easier "save these, delete rest" UX for clips
- Smaller min-width on waveform, let transcript take more space if needed
- Editable transcript
- Top bars need better UI, weird spacing, Anki Deck Name needs a better home
- Transcript has 2 borders, square and rounded
- How to handle upgrading to new version? How to record version number?
- Quick Export not responsive... did it actually export? (It does, but doesn't "feel like it")
- Audio sometimes clips early when playback (like when playing suggested clips on loop). This makes it appear as if the audio is clipped early, but appears to be a problem with the in-app audio playback; the final product was correct when played in Anki