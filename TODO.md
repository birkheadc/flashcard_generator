# TODO

- Fix waveform to match design better
  - Space bars slightly
  - Bars should always be same width; instead of widening out on zoom, should give more fidelity
  - Maximum zoom should be set by how fine the bars can go, don't let zoom in if there is no more detail to show
  - Have it render a second or w/e past the start and end, so that the first tick (0) and last tick can render, instead of having to hide them

- Icons

- Bottom bar padding